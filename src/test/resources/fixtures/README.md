# Certificate fixture provenance

The hostname, OpenSSL-validation, concurrency, and proxy-rejection certificate
and CRL fixtures in this directory were selected from the historical
`glite.security.util-java` test corpus. They remain solely as inputs for focused
protocol and validator tests. The original EGEE attribution and Apache License
2.0 notice are retained in the repository's `LICENSE.txt`.
